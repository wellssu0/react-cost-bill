import React, { memo } from 'react'
import { connect } from 'react-redux'

const ViewCategroyTab = props => {
  return(
    <div>ViewCategroyTab</div>
  )
}

export default memo(connect(null,null)(ViewCategroyTab))