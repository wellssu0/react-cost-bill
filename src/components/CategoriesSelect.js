import React, { memo } from 'react'
import { connect } from 'react-redux'

const CategoriesSelect = props => {
  return(
    <div>CategoriesSelect</div>
  )
}

export default memo(connect(null,null)(CategoriesSelect))